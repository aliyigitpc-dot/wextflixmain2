// WextFlix is served as a static site from /app/frontend/public/.
// The React bundle is intentionally a no-op — index.html has no #root.
const root = document.getElementById("root");
if (root) {
    // Fallback (kept harmless): if a #root ever appears, do nothing.
}
